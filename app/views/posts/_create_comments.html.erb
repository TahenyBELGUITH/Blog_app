 <%= form_with url: "users/#{@user.id}/posts/#{@post.id}/comments", scope: :comments do |form| %>
  <%= form.text_area :text, placeholder: "Type something here...", class:"placeholding-area" %>
  <%= form.submit "Comment", class:"btn btn-success" %>
<% end %>
